{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "DOCKER",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "",
    "healthCheckPath": "/",
    "restartPolicyType": "always",
    "restartThreshold": 60
  }
}
